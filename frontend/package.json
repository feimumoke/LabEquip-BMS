{
  "name": "labequip-bms-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.3.0",
    "antd": "^4.24.0",
    "@ant-design/icons": "^4.8.0",
    "axios": "^0.27.2",
    "dayjs": "^1.11.5",
    "mobx": "^6.6.2",
    "mobx-react-lite": "^3.4.0"
  },
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider HOST=www.equipbms.com react-scripts start",
    "build": "NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
    "test": "NODE_OPTIONS=--openssl-legacy-provider react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "react-scripts": "4.0.3"
  },
  "proxy": "http://localhost:8080"
}
